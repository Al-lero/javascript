const name = "Alero";

function speak(params) {
    let language = "English"
     
    function eat(params) {
        let food = "rice";
        console.log(food)
        console.log(name);
    }
    eat();
}
speak();